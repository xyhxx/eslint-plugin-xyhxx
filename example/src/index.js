function test(val) {
  if (
    val > 10
    || val < 100
  ) return '';
}

const obj = {a: 1, b: 2, c: 3, d: 4};

const {
  a,
  b,
  c,
  d,
} = obj;

function demo({
  a,
  b,
}) {
// 123
}
